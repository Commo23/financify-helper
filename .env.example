# Supabase (required for Rate Explorer page - scraping Edge Functions)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-or-publishable-key

# Optional: Supabase project ID (for reference)
# VITE_SUPABASE_PROJECT_ID=your-project-id

# --- Scraping (Rate Explorer) ---
# The Edge Functions (scrape-rates, scrape-irs, scrape-bonds) use Firecrawl.
# Set in Supabase Dashboard: Project Settings → Edge Functions → Secrets:
#   FIRECRAWL_API_KEY = your Firecrawl API key (https://firecrawl.dev)
# Without this secret, scraping will return "Firecrawl not configured".
